# Development / test dependencies (install with: pip install -r requirements-dev.txt)
-r requirements.txt

pytest>=8,<9
httpx>=0.27,<0.28    # FastAPI/Starlette 0.27 TestClient needs httpx <0.28 (0.28 removed the app= arg)
